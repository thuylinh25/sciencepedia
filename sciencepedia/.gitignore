# Dependencies & build output
node_modules
.next
out
build
next-env.d.ts
tsconfig.tsbuildinfo

# Bí mật — KHÔNG BAO GIỜ commit
.env
.env*.local
!.env.example
*.pem

# Log (build.log, server.log sinh ra khi chạy local)
*.log

# Hạ tầng / công cụ
.vercel
.DS_Store
/prisma/*.db
/meili_data

# Script dùng một lần khi khảo sát/nhập liệu — không thuộc về repo
scripts/tmp-*

# Bản sao xung đột OneDrive sinh khi hai máy cùng sửa — bản cũ, không phải nguồn
*-DESKTOP-*

# Thư mục .next bị dời sang một bên khi cache kẹt (Windows + OneDrive).
# Xoá thường thất bại vì I/O error, nên ít nhất đừng để chúng vào repo.
.next-*
.next.*

# Thư mục dàn của `covers:build`, `covers:bodies` và `images:variants`. Ảnh thật
# sống trên Cloudflare R2 (src/lib/asset.ts); đây chỉ là bản dựng ra chờ tải lên,
# không phải nguồn — dựng lại được bất cứ lúc nào từ chính các script ấy.
/assets
